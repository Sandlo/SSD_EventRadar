package com.example.eventradar.adapters

import com.example.eventradar.R

/**
 * Ein spezialisierter Adapter, der einen Fehlerzustand in einer RecyclerView anzeigt.
 */
class ErrorAdapter : MessageAdapter(R.layout.list_item_error)
// Keine zusätzlichen Methoden oder Logik benötigt, da die Logik in MessageAdapter definiert ist.
